<UserControl
    x:Class="DSAMetatalente.Controls.ResultControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:gb="using:UnoGroupBox"
    xmlns:local="using:DSAMetatalente.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <gb:GroupBox Header="Würfelergebnisse" Theme="Fluent">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="10,7,10,0"
                Text="Würfelergebnisse:" />
            <TextBlock
                Name="StringDiceResult"
                Grid.Row="0"
                Grid.Column="1"
                Margin="10,7,10,0" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Margin="10,10,10,0"
                Text="Punkte übrig behalten:" />
            <TextBlock
                Name="StringPointsResult"
                Grid.Row="1"
                Grid.Column="1"
                Margin="10,10,10,0" />
            <TextBlock
                Name="StringTextResult"
                Grid.Row="2"
                Grid.Column="0"
                Grid.ColumnSpan="2"
                Margin="10"
                AllowDrop="False"
                TextWrapping="Wrap" />
        </Grid>
    </gb:GroupBox>
</UserControl>
