<TabViewItem
    x:Class="DSAMetatalente.Views.TabPlantSeeking"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:DSAMetatalente.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DSAMetatalente.ViewModels"
    Header="Kräuter suchen"
    IsClosable="False"
    mc:Ignorable="d">
    <TabViewItem.DataContext>
        <vm:TabPlantSeekingViewModel />
    </TabViewItem.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Margin="10"
            Text="Dauer" />
        <NumberBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="10"
            IsEnabled="{Binding CanRoll}"
            Minimum="{Binding MinDuration}"
            SpinButtonPlacementMode="Compact"
            Value="{Binding Duration, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <Button
            Grid.Row="0"
            Grid.Column="2"
            Width="100"
            Height="30"
            Margin="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Command="{Binding RollCommand}"
            Content="Würfeln"
            IsEnabled="{Binding CanRoll}" />
        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Margin="10"
            Text="Pflanze" />
        <CheckBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="10"
            VerticalAlignment="Center"
            Content="Zufall"
            IsChecked="{Binding Coincidence}"
            IsEnabled="{Binding CanRoll}" />
        <ComboBox
            Grid.Row="1"
            Grid.Column="2"
            Margin="10"
            IsEnabled="{Binding CanRoll}"
            ItemsSource="{Binding Plants}"
            SelectedItem="{Binding CurrentPlant, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <Grid
            Grid.Row="3"
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="5"
                Text="Bestimmung:" />
            <TextBlock
                Grid.Row="0"
                Grid.Column="1"
                Margin="5"
                Text="{Binding IdentificationMod}" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Margin="5"
                Text="Vorkommen:" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="1"
                Margin="5"
                Text="{Binding Occurrence}" />
            <TextBlock
                Grid.Row="2"
                Grid.Column="0"
                Margin="5"
                Text="Grundmenge:" />
            <TextBlock
                Grid.Row="2"
                Grid.Column="1"
                Margin="5"
                Text="{Binding Loot}"
                TextWrapping="Wrap" />
        </Grid>
        <controls:ResultControl
            Grid.Row="4"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            DiceResult="{Binding DiceResult}"
            PointsResult="{Binding PointsResult}"
            TextResult="{Binding TextResult}" />
    </Grid>
</TabViewItem>
