<TabViewItem
    x:Class="DSAMetatalente.Views.TabHunting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:DSAMetatalente.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DSAMetatalente.ViewModels"
    Header="Jagen gehen"
    IsClosable="False"
    mc:Ignorable="d">
    <TabViewItem.DataContext>
        <vm:TabHuntingViewModel />
    </TabViewItem.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Margin="10"
            Text="Dauer" />
        <NumberBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="10"
            IsEnabled="{Binding CanRoll}"
            Minimum="{Binding MinDuration}"
            SpinButtonPlacementMode="Compact"
            Value="{Binding Duration, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <Button
            Grid.Row="0"
            Grid.Column="2"
            Width="100"
            Height="30"
            Margin="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Command="{Binding RollCommand}"
            Content="Würfeln"
            IsEnabled="{Binding CanRoll}" />
        <RadioButton
            Grid.Row="1"
            Grid.Column="0"
            Command="{Binding ToggleHuntingVariantCommand}"
            CommandParameter="Pirschjagd"
            Content="Pirschjagd"
            GroupName="huntingVariant"
            IsEnabled="{Binding CanRoll}" />
        <RadioButton
            Grid.Row="2"
            Grid.Column="0"
            Command="{Binding ToggleHuntingVariantCommand}"
            CommandParameter="Ansitzjagd"
            Content="Ansitzjagd"
            GroupName="huntingVariant"
            IsEnabled="{Binding CanRoll}" />
        <CheckBox
            Grid.Row="1"
            Grid.Column="2"
            Margin="10,0,0,0"
            Content="Scharfschütze"
            IsChecked="{Binding IsScharfschuetze}"
            IsEnabled="{Binding CanRoll}" />
        <CheckBox
            Grid.Row="2"
            Grid.Column="2"
            Margin="10,0,0,0"
            Content="Meisterschütze"
            IsChecked="{Binding IsMeisterschuetze}"
            IsEnabled="{Binding CanRoll}" />
        <TextBlock
            Grid.Row="3"
            Grid.Column="0"
            Margin="10"
            Text="Tier" />
        <CheckBox
            Grid.Row="3"
            Grid.Column="1"
            Margin="10"
            VerticalAlignment="Center"
            Content="Zufall"
            IsChecked="{Binding Coincidence}"
            IsEnabled="{Binding CanRoll}" />
        <ComboBox
            Grid.Row="3"
            Grid.Column="2"
            Margin="10"
            IsEnabled="{Binding CanRoll}"
            ItemsSource="{Binding Animals}"
            SelectedItem="{Binding CurrentAnimal, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <TextBlock
            Grid.Row="4"
            Grid.Column="0"
            Margin="10"
            Text="Waffe" />
        <NumberBox
            Grid.Row="4"
            Grid.Column="1"
            Margin="10"
            IsEnabled="{Binding CanRoll}"
            Minimum="0"
            SpinButtonPlacementMode="Compact"
            Value="{Binding SkillWeapon}" />
        <ComboBox
            Grid.Row="4"
            Grid.Column="2"
            Margin="10"
            IsEnabled="{Binding CanRoll}"
            ItemsSource="{Binding Weapons}"
            SelectedItem="{Binding UsedWeapon, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <Grid
            Grid.Row="5"
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Margin="5"
                Text="Jagdschwierigkeit:" />
            <TextBlock
                Grid.Row="0"
                Grid.Column="1"
                Margin="5"
                Text="{Binding Difficulty}" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Margin="5"
                Text="Beute:" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="1"
                Margin="5"
                Text="{Binding Loot}"
                TextWrapping="Wrap" />
        </Grid>
        <controls:ResultControl
            Grid.Row="6"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            DiceResult="{Binding DiceResult}"
            PointsResult="{Binding PointsResult}"
            TextResult="{Binding TextResult}" />
    </Grid>
</TabViewItem>