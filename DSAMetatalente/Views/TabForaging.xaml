<TabViewItem
    x:Class="DSAMetatalente.Views.TabForaging"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:DSAMetatalente.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:DSAMetatalente.ViewModels"
    Header="Nahrung suchen"
    IsClosable="False"
    mc:Ignorable="d">
    <TabViewItem.DataContext>
        <vm:TabForagingViewModel />
    </TabViewItem.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Margin="10"
            Text="Dauer" />
        <NumberBox
            Grid.Row="0"
            Grid.Column="1"
            Margin="10"
            VerticalAlignment="Top"
            IsEnabled="{Binding CanRoll}"
            Minimum="{Binding MinDuration}"
            SpinButtonPlacementMode="Compact"
            Value="{Binding Duration, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        <Button
            Grid.Row="0"
            Grid.Column="2"
            Width="100"
            Height="30"
            Margin="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Command="{Binding RollCommand}"
            Content="Würfeln"
            IsEnabled="{Binding CanRoll}" />
        <controls:ResultControl
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            DiceResult="{Binding DiceResult}"
            PointsResult="{Binding PointsResult}"
            TextResult="{Binding TextResult}" />
    </Grid>
</TabViewItem>
